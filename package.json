{
  "name": "react-form-generator",
  "version": "0.0.1",
  "private": true,
  "description": "A react rule-based renderer, useful on generating dynamic forms",
  "keywords": [
    "react",
    "renderer",
    "rule-based"
  ],
  "scripts": {
    "dev": "pnpm -r  --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r --filter './playground' run start",
    "test": "pnpm -r --filter './rule-based-renderer' run test"
  },
  "author": "Nairman Movaffaghi",
  "license": "MIT",
  "packageManager": "pnpm@10.6.3"
}
